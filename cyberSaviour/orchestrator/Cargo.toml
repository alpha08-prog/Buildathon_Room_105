[package]
name    = "cybersaviour-orchestrator"
version = "0.1.0"
edition = "2021"

[dependencies]
serde       = { version = "1", features = ["derive"] }
serde_json  = "1"
crossbeam-channel = "0.5"

[profile.release]
opt-level = 3
strip     = true   # strip debug symbols → smaller binary
